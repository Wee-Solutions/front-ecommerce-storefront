/**
 * Default design token values (mirrors `default.css`).
 * Per-store overrides live in `src/theme/stores/*.theme.json`.
 */
export const DEFAULT_STOREFRONT_TOKENS = {
  sf: {
    pageBg: "#faf4f2",
    primary: "#3d2f35",
    primaryFg: "#fdf8f6",
    accent: "#b76e79",
    accentFg: "#ffffff",
    muted: "#8a787c",
    surface: "#f5e8e4",
    border: "#e8d5d0",
    radius: "0.875rem",
  },
  radius: "0.625rem",
} as const;
